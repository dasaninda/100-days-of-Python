#Data types

#STRING
#print ("Hello"[4])

#INTEGER
#print (123 + 456)

#FLOAT
#3.13333

#BOOLEAN
#True
#False

#Practise-1
#num_char = len(input("What is name?"))
#new_num_char = str(num_char)
#print ("Your name has " + new_num_char + " characters")


#Q1 Write a program that adds digits in 2 digit number. Eg- if input is 35, output is 3+5 = 8

#two_digit_number = input ("Type a two digit number:")
#first_digit = int(two_digit_number[0])
#second_digit = int(two_digit_number[1])
#result = first_digit + second_digit
#print (result)


#Q2 Write a program to find the BMI with weight and heigh input from user

#height = input ("What is your height in m: ")
#weight = input ("What is your weight in kg: ")
#BMI = int(weight) / float (height) ** 2  #this line turns the input into int and float
#bmi_as_int = int(BMI) #this turns the result into a whole number
#print (bmi_as_int)


#Q3 Write a program to find how many days, weeks, months you have to live 

#age = input ("What is your age?: ")
#age_int = int(age)
#years_remaining = 90 - age_int
#days_remainning = years_remaining * 365
#weeks_remaining = years_remaining * 52
#months_remaining = years_remaining * 12
#message = f"you have {days_remainning} days, {weeks_remaining} weeks, and {months_remaining} months remaining till death"
#print (message)


### DAY 2 Project ###
#1 Create a tip calculator from users inputs 
print ("Welcome to the tip calculator")
bill = float (input ("What was the total bill? $"))
tip = int(input ("How much tip would you like to give? 10, 12, or 15?"))
people = int(input ("How many people to split the bill? "))
tip_percent = tip / 100
total_tip = bill * tip_percent
total_bill = bill + total_tip
bill_per_person = total_bill / people
final_amount = round(bill_per_person, 2)
print (f"Each person should pay {final_amount}")





